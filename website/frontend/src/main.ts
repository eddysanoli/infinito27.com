import { createApp } from 'vue';
import './style.css';
import App from './App.vue';

// Insert the "App" vue component into the div with ID "app".
// Said div is located in the index.html file
createApp(App).mount('#app');
